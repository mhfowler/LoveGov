# create users add to admin group and ssh group
useradd max 
groupadd admin
groupadd sshusers
usermod -a -G admin max
usermod -a -G sshusers max

# make folders for sites
mkdir /srv/dev
mkdir /srv/live

# install subversion
apt-get install subversion

# checkout project to dev and live
svn checkout https://subversion.assembla.com/svn/modernpolitics/subversioned /srv/dev
svn checkout https://subversion.assembla.com/svn/modernpolitics/subversioned /srv/live

# install python
apt-get python

# install apache2 + mod_wsgi
apt-get install apache2
apt-get install  libapache2-mod-wsgi

# change apache2.conf to include all conf files from /srv/dev/server/apache

# change ~/.bashrc to source /srv/dev/server/shared.bashrc

# add group access and user wsgi_process
groupadd access
useradd wsgi_process

# install memcached
apt-get memcached

# install pip to help install dependencies
apt-get install python-pip python-dev build-essential 
apt-get install git

# install dependencies
pip install Django
pip install PIL # didntwork
# hadtodothis: apt-get install python-imaging python-imaging-tk
pip install sorl-thumbnail
pip install django_facebook
pip install python-memcached
pip install BeautifulSoup
pip install xlrd
pip install -e git+https://github.com/toastdriven/django-haystack.git@master#egg=django-haystack
pip install django-debug-toolbar
pip install Whoosh




# install mysql
apt-get install mysql-server
apt-get install python-mysqldb

# create folders
mkdir /static
mkdir /static/dev
mkdir /static/live
mkdir /media
mkdir /media/dev
mkdir /media/live
mkdir /log
mkdir /log/dev
mkdir /log/live
mkdir /dump

# set mysql permissions
create user 'wsgi_process'@'localhost' identified by '~~~~~~~~~'; #didntwork, changed db user to root instead

# scp copy over /srv/live directory from old server to new 
# scp copy over /medai directory from old server to new

# dump live mysql data from old server
# scp copy file over to new server
# loaddata with live_manage in /srv/live on new server

# collectstatic and set all permissions for all folders
